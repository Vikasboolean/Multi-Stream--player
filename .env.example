# Frontend deployment
# Optional when the frontend and signaling server are deployed on the same host.
# Set this when the signaling server is deployed separately.
VITE_SIGNALING_URL=wss://your-signaling-server.example.com

# Signaling server deployment
# Most hosts set PORT automatically. Keep this for local/server deployments that need it.
PORT=3001
